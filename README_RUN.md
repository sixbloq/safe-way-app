```
# Run instructions


1. Install dependencies:
npm install


2. Start Expo Metro bundler:
npm start


3. Open the app in Expo Go (mobile) or run in emulator with:
npm run android
npm run ios


Notes:
- This prototype uses a simulated backend stored in AsyncStorage.
- To c